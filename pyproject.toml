[project]
name = "kernelgarage"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "kernelgarage", email = "thekernelgarage@gmail.com" }
]
requires-python = ">=3.14"
dependencies = []

[project.scripts]
kernelgarage = "kernelgarage_sdk:main"

[build-system]
requires = ["uv_build>=0.9.16,<0.10.0"]
build-backend = "uv_build"
